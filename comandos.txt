criar ambiente virtual
pyton -m venv venv

ativando o ambiente virtual
venv\scripts\activate

pip install flask

desativar ambiente virtual
deactivate

gerar a lista de dependencias
pip freeze > requirements.txt

pip install -r requirements.txt